# Prod overrides — chargé uniquement en mode release
# (utilisé par `pnpm --filter @tot/firetv build:local` et `eas build`)
# Surcharge .env.local : seules les variables d\'API/clés "release" doivent y être.
EXPO_PUBLIC_API_BASE_URL=https://tractiveontv-api.vercel.app
